import GenericStep from './GenericStep';
import * as CSS from 'csstype';

const StepTextStyle: CSS.Properties<string | number> = {
	fontSize: GenericStep.defaultProps.style.fontSize
};

export { StepTextStyle };
